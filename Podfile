# Uncomment the next line to define a global platform for your project
platform :ios, '12.2'

target 'manageme' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for manageme
  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  target 'managemeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'managemeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
